# GÜN SONU — OPUS 5 turu (2026-08-02)

> **Bu bir devir dosyasıdır.** `/clear` sonrası oturum buradan devam eder.
> Otorite sırası: `artifacts/current-work.json` → bu dosya → `docs/ai/OPUS5-TAVSIYELER.md`.

---

## 1. NE TUTTU — 10 commit, hepsi `main`'de, kapı her birinde yeşil

Test tabanı **2382 → 2443** (+61). Hiçbir test silinmedi, hiçbir sayı düşmedi.

| # | Commit | Ne yaptı |
|---|---|---|
| 1 | `1fa5dd8` | **Sistem haritası** — 6 paralel ajan, 8 katman, dosya:satır kanıtlı (`docs/ai/SISTEM-HARITASI.md`) |
| 2 | `962074a` | **Hasat kanalı onarıldı** — stderr → stdout; meta-duvar artık `.mjs` hook'ları da görüyor |
| 3 | `cabce9b` | **`scripts/baglar.mjs`** — belgelerdeki her `dosya:satır` atfının doğrulayıcısı |
| 4 | `0cde42f` | Terra 5.6'nın haritada bulduğu 5 kusur onarıldı + duvar sertleştirildi |
| 5 | `ce0e7a0` | **`motion-lint` duvara bağlandı** + `.md` teslim dosyaları artık ölçülüyor |
| 6 | `65ac364` | **`teslim-denetim.mjs` yeniden yazıldı** — 20 projeye yanlış sayı basıyordu |
| 7 | `b42056f` | 28 kırık belge atfı onarıldı, denetçi kapıya bağlandı — **KIRIK 0** |
| 8 | `4357d17` | Tavsiye belgesi gerçek duruma çekildi, T13 eklendi |
| 9 | `8d36764` | Terra'nın 5 kusuru daha: **iki YANLIŞ BULGU geri alındı**, 3 sömürü yolu kapatıldı |
| 10 | `a18cd7d` | Belgedeki yanlış bulgular ve sayılar düzeltildi |

---

## 2. DELİKLER — kapandı mı, nasıl kapandı

**Kapandı (kökten, yama değil):**

1. **Hasat bulguları modele hiç ulaşmıyordu.** 12 çıktı çağrısının 12'si stderr'deydi;
   SessionStart'ta modele yalnız stdout girer. Ölçüm önce stdout 0/stderr 1692, şimdi
   **stdout 1692 / stderr 0**. Kapı açılır açılmaz üç gerçek bekleyen iş bildirdi.
2. **Teslim doğrulayıcısı 20 projenin 19'una yanlış cevap veriyordu.** `argv` almıyordu,
   hedefi koda gömülüydü. Yeniden yazıldı; 36 test.
3. **`motion-lint` hiçbir kapıya bağlı değildi** — 490 satır, 14 kural, kendi testi var.
   Üstelik kendi yorumunda "gate.sh parse ediyor" yazıyordu. Artık gerçekten bağlı.
4. **`.md` teslim dosyaları kapıdan hiç ölçülmeden geçiyordu** (prompt VE motion tarafında).
5. **Belgelerdeki 28 kırık atıf** — taşınmış script, silinmiş yapı, donmuş satır numarası.
   Hepsi onarıldı, denetçi commit duvarında, **KIRIK 0**.
6. **Meta-duvarın kör noktası** — yalnız `.sh` hook'larını doğruluyordu, yani modele
   *gerçekten ulaşan* iki hook (`buddy.mjs`, `oturum-durumu.mjs`) hiç kontrol edilmiyordu.

**Ölçüldü ama açık bırakıldı (Mami kararı — bkz. §4).**

---

## 3. BU TURUN EN ÖNEMLİ DERSİ

**Yeni yazdığım doğrulayıcı, eskisinin kusur sınıfını yeniden üretti.**

Eski `teslim-denetim` hattı `### K` regex'iyle okuyordu ve kördü. Yenisi `_PROMPTLAR` adıyla
okudu ve **yine kör oldu** — `Kütle ve Ağırlık`ın kalan 27 karesi `_CODEX-KALAN-START-FRAMELER.txt`
adlı dosyadaydı. 26 test yeşildi. İki yanlış bulgu Mami'ye rapor edildi.

Farkı **ikinci göz** yaptı (Terra 5.6 High), yeşil test değil.

Kök neden onarıldı: prompt kaynağı artık **adıyla değil İÇERİĞİYLE** bulunuyor
(`STYLE:` / `NEGATIVE:` taşıyan her dosya). Bu kıstas `prompt-lint.mjs:815`'te **zaten vardı**
ve kopyalanmamıştı — iki doğrulayıcının aynı hattı iki farklı sözleşmeyle okuması,
bu turun ana kusur sınıfı.

**Kural olarak yazıldı:** `docs/ai/MODEL-YONLENDIRME.md` R4 — dış modelin hiçbir iddiası
doğrulanmadan kabul edilmez; Terra'nın da bir iddiası yanlış çıktı ve grep'le elendi.

---

## 4. MAMİ'NİN KARARINI BEKLEYEN — dördü de kalite tercihi, ben seçemem

| # | Karar | Neden ben seçemem |
|---|---|---|
| **T6** | **Motion kelime bandının kanonik sayısı.** Yasa `:546`=190-215, yasa `:561`=210-260, `motion-lint:137`=160-250/180-225. Üçü de "yasa" diye okunuyor. | Sayıyı seçmek ölçülmüş bir zevk kararı |
| **T7** | **Ajan brief'i yetenek olsun** (`.claude/agents/`). Ama `sekans-denetim`'in kıstas sırası çelişik: `director:158` VO-uyumuyla başlıyor, `denetim:38` FİKİR ile. | Hangisi önce gelecek = kalite kararı |
| **T8** | **Yasaya makine-okur etiket** — her lint kuralı hangi §'i ölçtüğünü beyan etsin. | Yasa metnine dokunmak Mami'nin |
| **T9** | **Hangi skill yüzeyi kanon.** `~/.claude/skills` üç skill'de proje sürümünü **eziyor** (buddy 324 vs 178). C1 düzeltmesinin iki gün sonra geri dönmesinin sebebi bu. | Nüsha silmek geri alınamaz |

Ayrıca **izin kararı:** AGY headless düşünüyor ama `read_file` izni reddedildiği için
**okuyamıyor**. Açmak `settings.json` izin ayarı — dokunulmadı.

---

## 5. ÜRETİMDE DURAN GERÇEK İŞ

🔴 **`5. Sınıf - Destek ve Hareket Sistemi` — K43-K52 hiç yazılmamış.**
VO 52 cümle, prompt 41 kare. Dizinde `PROMPTLAR/A-K01-K14` · `B-K15-K28` · `C-K29-K41` var,
D bloğu yok. Terra bağımsız doğruladı. **Bu iş bu hâliyle eksik teslim edilecekti.**

**Aktif iş kaydı hâlâ `5. Sınıf - Farklı Kültürler`** (faz: enzim, kit eksiği MOTION +
KABA-KURGU.xml). 7 kare Mami'de: K03 · K08 · K12 · K17 · K32 · K46 · K49 (+K29 opsiyonel).
K49 kapanış karesi referansı `2.png`e bağlanır, `49.png`e değil.

**Hasat kapısı üç bekleyen iş bildiriyor** (artık görünür):
`Eşeyli ve Eşeysiz Üreme [ERROR]` · `Kuvvet MİRA [ERROR]` ·
`Kuvvet ve Kuvvetin Ölçülmesi [STALE_N]`.

---

## 6. YENİ ARAÇLAR — clear sonrası bunları bil

```bash
node scripts/teslim-denetim.mjs --all              # 20 proje, biçim + sayım + VO örtmesi
node scripts/teslim-denetim.mjs "<proje yolu>"     # tek proje
node scripts/baglar.mjs                            # belgelerdeki atıflar (kapıda --strict)
```

Kadro çağrı biçimleri **ölçüldü** ve `docs/ai/MODEL-YONLENDIRME.md`'de:
`codex exec` (Sol) · `codex exec -c model=gpt-5.6-terra -c model_reasoning_effort=high` (Terra) ·
`agy --model gemini-3.1-pro-high -p` (okuyamıyor, izin bekliyor).

---

## 7. SIRADAKİ TEK ADIM

Mami dört kararı verir → T7 (ajan brief'leri kalıcı yetenek) uygulanır.
Karar gelmezse geri alınabilir olanlara devam: `ONAY-BEKLEYEN.md:3` yalanı, öksüz iki hafıza
kaydı, `artifacts/` ölü raporları.
