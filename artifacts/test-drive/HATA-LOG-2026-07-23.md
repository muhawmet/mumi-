# HATA LOG — Test Sürüşü 2026-07-23

**Mod:** KAYDET, ONARMA. Fix ayrı oturumda.
**Baseline:** HEAD `4c0c79d` (push'lu, temiz) · tsc 0 · vitest 2046/2046 · build ✓ · E2E 15/15
**Hedef:** 6-7 video · her videoya SENARYO girilecek (konu yazıp geçme → klon-motion bloklaması)

Sınıflar: `BLOKER` (üretim tamamen durdu) · `CIDDI` (çıktı bozuk ama akış devam) · `KOZMETIK` (rahatsız edici, iş görüyor)

---

## Üretim turları

| # | Video / konu | Dünya | Motor | Durum |
|---|---|---|---|---|
| 1 | — | — | — | bekliyor |

---

## Bulgular

### H-01 · [BLOKER] · BASLAT.command çalışmıyor — execute bit yok (644)

- **Adım:** Başlangıç — Mac'te `BASLAT.command` çift tıklama (üretim daha başlamadan)
- **Dünya / Motor:** —
- **Beklenen:** Terminal açılır, launcher koşar
- **Olan:** macOS diyalogu: *"Uygun erişim ayrıcalıklarına sahip olmadığınız için BASLAT.command dosyası çalıştırılamadı."*
- **Tekrar ediyor mu:** her seferinde (kalıcı dosya izni durumu)
- **Üretimi durdurdu mu:** EVET — Mac'te launcher hiç açılmıyor
- **Log / ekran:**
```
$ ls -la BASLAT.command
644  BASLAT.command  876B     ← 755 olmalı
```
- **Not:** Repo'da mode `100644` olarak duruyor. Windows birincil ortam olduğu için Mac
  execute-bit'i commit'lerde korunmamış olabilir. Launcher-parity sözleşmesi (CRLF için .bat)
  var ama Mac tarafında mode kontrolü yok gibi. **Fix DEĞİL, iz.**
- **ÇÖZÜLDÜ (Mami onayıyla):** `chmod +x BASLAT.command` → 755. Git'te `100644 → 100755` mode
  diff'i duruyor, commit kararı gün sonunda.

### H-02 · [CIDDI] · "Komut export kapalı" mesajı yanıltıcı — coverage yazıyor, hash'ten kapanıyor

- **Adım:** Timeline/Export barı — "Komut JSON" butonu gri, altında sarı uyarı
- **Dünya / Motor:** —
- **Beklenen:** Kapının gerçek sebebini söyleyen mesaj (hash uyuşmazlığı)
- **Olan:** *"Komut export kapalı — Kaynak hazır değil: Kaynak bütünlüğü 100%; %100 gerekli."*
  → kendi içinde çelişkili: %100 var, %100 isteniyor, yine de kapalı.
- **Tekrar ediyor mu:** kapı her kapandığında (koşul-bağımlı, deterministik)
- **Üretimi durdurdu mu:** EVET — Komut JSON indirilemiyor, runner çalıştırılamıyor (bkz. H-03)
- **Log / ekran:**
```
Komut export kapalı — Kaynak hazır değil: Kaynak bütünlüğü 100%; %100 gerekli.
```
- **Not (iz, fix değil):** [useStudioStore.ts:425](../../src/store/useStudioStore.ts#L425)
  kapıyı `!s.sourceReport.ok` ile kapatıyor ama mesaja `coverage` basıyor. `ok` ise
  [qa.ts:306](../../src/core/qa.ts#L306) uyarınca **rawHash ≠ reconHash** olunca false —
  coverage %100 olsa bile. Kapı DOĞRU (qa.test.ts:247 bu kapıyı bilerek koymuş: bozuk kaynak
  "%100" yazıp export açıyormuş). **Kırık olan mesaj, kapı değil.** Kullanıcıya "metni
  değiştirmeden yeniden ingest et" demeli (DashboardStep.tsx:195 doğru metni zaten biliyor).

### H-03 · [BLOKER·türev] · MOTION-CALISTIR: COMMAND-INBOX'ta Proje Paketi var, Komut JSON yok

- **Adım:** `agents/MOTION-CALISTIR.command` — proje "6. Sınıf – Kuvvetlerin Güç Birliği"
- **Beklenen:** Runner komutu bulup motion fazını koşar
- **Olan:** `COMMAND-INBOX içinde çalıştırılabilir MAMILAS Command yok.`
  Atlanan dosya: `.mamilas-project.json (mamilas.project-pack.v1)`
- **Tekrar ediyor mu:** H-02 kapısı açılana kadar her seferinde
- **Üretimi durdurdu mu:** EVET
- **Log / ekran:**
```
❌ COMMAND-INBOX içinde çalıştırılabilir MAMILAS Command yok.
   Dosyayı buraya bırak: /Users/Muhammet/Desktop/mamilas-modern/agents/COMMAND-INBOX
   Timeline → "Komut JSON" indir. "Proje Paketi" bu runner için çalıştırılamaz.
   Atlanan dosyalar: -.mamilas-project.json (mamilas.project-pack.v1)
```
- **Not:** Runner davranışı DOĞRU ve mesajı net. Bu bulgu H-02'nin türevi — Komut JSON
  indirilemediği için Proje Paketi bırakılmış. H-02 açılınca kendiliğinden çözülür.
  Tek gerçek soru: kullanıcı "Proje Paketi"ni runner'a bırakmaya neden yöneliyor (UI'da
  ikisi yan yana, biri kapalı) — ergonomi izi.

---

## H-02 ÇÖZÜMÜ (Mami direktifi — "manuel kestiysem oraya karışmasın, kilitlemesin")

**Kök:** `sourceIntegrity` sahne metinlerini birleştirip ham kaynakla **birebir** karşılaştırıyordu.
Mami 60 sahneyi elle 69'a böldü + sığmayan boş satırları sildi → recon ≠ raw → `ok=false` →
Komut JSON kilitlendi. Sistem **kasıtlı yönetmen edit'ini "bozulma" sanıyordu.**

**Karar:** Manuel beat modunda beat'ler ZATEN yönetmenin istediği kesim — ham metin onları
ölçecek otorite değil. Otomatik modlarda kapı duruyor (orada uyuşmazlık = gruplama metin kaybetti).

**Dokunulan yerler:**
- `src/store/useStudioStore.ts:419` — `sourceReadiness` artık `beatMode` alıyor; `Manuel` ise
  bütünlük kapısı hiç çalışmıyor. `commandAuthoringReadiness` + `productionReadiness` imzaları
  `beatMode` taşıyacak şekilde genişletildi.
- `src/core/qa.ts:306` — ENCYCLOPEDIA integrity kontrolü Manuel modda kırmızı yakmıyor;
  ayrıca hash-vetosu (`rawHash === reconHash`) kalktı, otorite `ok`.
- `src/core/source.ts:516` — `wordSkeleton` eklendi: boşluk düzeni farkı bozulma sayılmıyor
  (kelimeler aynıysa `ok`). Bu Manuel'den bağımsız, otomatik modda da geçerli.
- `AppLayout.tsx:76` + `DashboardStep.tsx:39` — uyarı bandı Manuel modda susuyor.

**Kapı:** tsc 0 · vitest 2046/2046 · build ✓ (bozuk-kaynak testi dahil hepsi yeşil).
**Commit YOK** — gün sonunda Mami karar verecek. `BASLAT.command` mode değişikliği de bekliyor.

**Açık risk (kayıt için):** Manuel modda kaynak bütünlüğü artık hiç doğrulanmıyor. Sahne metni
kaynaktan gerçekten saparsa (kopyala-yapıştır kazası) sistem uyarmaz. Mami'nin bilinçli kararı.

**SONUÇ:** Komut JSON indi, runner komutu KABUL etti (proje klasörü + runs/ dizini açıldı).
H-02 ve H-03 kapandı. Akış bir sonraki duvara ilerledi → H-04.

---

### H-04 · [BLOKER] · MOTION-CALISTIR çift tık: `--director` var, `--provider` yok

- **Adım:** `agents/MOTION-CALISTIR.command` çift tık — komut kabul edildi, provider seçiminde durdu
- **Dünya / Motor:** Proje "6. Sınıf – Kuvvetlerin Güç Birliği"
- **Beklenen:** Yönetmen modu açılır, batch arkada koşar
- **Olan:** `mamilas-command: --director için --provider claude|codex zorunlu`
- **Tekrar ediyor mu:** her çift tıkta (yapısal — argüman eksik)
- **Üretimi durdurdu mu:** EVET
- **Log / ekran:**
```
Proje klasörü: .../MAMILAS-PROJELER/6. Sınıf – Kuvvetlerin Güç Birliği
Aktif çalışma: .../runs/mamilas-fa5f34c654a6cd655ed4b8e87e4402027a2b4ee4dfd7991573a40685e436880b
mamilas-command: mamilas-command: --director için --provider claude|codex zorunlu
```
- **Not (iz, fix değil):** [MOTION-CALISTIR.command](../../agents/MOTION-CALISTIR.command) son satırı
  `exec node runner.mjs --director "$@"` — `--provider` YOK. Runner
  [runner.mjs:188-190](../../agents/runner.mjs#L188) mantığı: tek CLI kuruluysa onu seçer, İKİ CLI
  kuruluysa interaktif olarak sorar, `stdin.isTTY` yoksa `die()`. Mac'te **claude ve codex İKİSİ de
  kurulu** (`/Users/Muhammet/.local/bin/claude`, `/opt/homebrew/bin/codex`) → seçim gerekiyor →
  çift tık ortamında soru sorulamıyor → ölüyor. **Launcher bu haliyle çift tıkla hiç çalışamaz.**
  Windows'ta tek CLI kurulu olduğu için orada sorun çıkmamış olabilir (launcher-parity izi).
- **Geçici yol (üretim akıtmak için):** `node runner.mjs --director --provider claude`
- **Mami kararı:** claude. **ÇÖZÜLDÜ** — ama kök launcher'da değildi:
  - `runner.mjs:259` — `--director` de `--launch` gibi açık çalıştırma niyeti sayılmıyordu. Çift
    tıkta TTY yok → `dryRun=true` → `launch=false` → provider hiç çözülmüyor ama `--director`
    argümanlarda kalıp runtime'a gidiyor → runtime "provider zorunlu" diye ölüyor.
  - `runner.mjs:217` (`approvePendingStoryboard`) — ön kontrol `--director`'ü de gönderiyordu;
    o dry-run'ın provider'a ihtiyacı yok. Artık `--batch` ile birlikte düşürülüyor.
  - Launcher'lara `--provider claude` sabitlendi (4 dosya: .command + .bat ikizleri, CRLF korundu,
    docsContract <20 satır sınırı içinde).
  - Doğrulama: `node runner.mjs --director --provider claude --project "..."` < /dev/null →
    provider hatası GİTTİ, akış "Sahne 1 storyboard onayı bekliyor"a ilerledi.

---

### H-05 · [BLOKER] · Komutta `locks.cast` boş — 69 sahnenin ana öznesi tanımsız

- **Adım:** batch → sahne 1 → image_author
- **Beklenen:** Mira'nın kimliği kilitlerden okunur, 69 kare aynı kişiye çapalanır
- **Olan:** `📦 Sahne 1/69 → DURDU: FACT_REQUIRED: Mira'nın kimlik gerçeği (yüz/cast)`
- **Tekrar ediyor mu:** yapısal — cast boş olduğu sürece her koşuda
- **Üretimi durdurdu mu:** EVET (sahne 1; sahne 2 devam etti ve PASS aldı)
- **Not:** Author/jüri DOĞRU davrandı — PROTOCOL.md:34 "tekrar eden özne + cast gerçeği yoksa
  kimlik UYDURULMAZ". Kusur davranışta değil, **verinin taşınmamasında**: Mami'nin `@mira`
  referans seti (5 varyant: mira / mira_ogrenci / mira_uye / mira_space / mira_komsu) harici
  araçta hazır ama komuta hiç girmemiş. `locks.cast: ""`, `refIds` yalnız stil ref'leri taşıyor
  (`pixar_dimensional`, `pixar_emotional_staging`, `soul`) — identity ref'i yok.
  Zincir sağlam (`commandExport.ts:271` `cast: state.cast`), yani site'de alan boş kalmış.
- **ÇÖZÜM (uygulandı):** Kimlik `--add-directive-file --scope PROJECT` ile LIVE_CHAT direktifi
  olarak bağlandı (`live-6582c96df5f2b9a4`). Elle JSON kurcalanmadı — `commandId` hash'i runtime
  tarafından yeniden hesaplandı (`mamilas-09369feb…7487e`), mühür sağlam. Direktif `shotApprovals`'ı
  sıfırladığı için 69 onay yeniden verildi.
- **AÇIK SORU (Mami'ye):** Kalıcı çözüm site'de `cast` alanının doldurulması. Ayrıca `@mira`
  gerçek bir **identity ref** olarak `refIds`'e girebilir mi — bugün yalnız serbest metin tarifi var.

---

## SOL'UN YAPISAL TEŞHİSİ (bağımsız denetim, kanıtlı)

**"Neden 2046 test yeşilken üretim hiç başlamadı?"**

Sol dünkü kırık kodu `git stash` ile geri koyup tüm süiti koşturdu → **2046/2046 PASS, 0 FAIL.**
Yani üretimi durduran kodun ta kendisiyle süit yeşil. Ölçüm sonrası stash geri alındı.

**Boşluğun adı: son-metre testsizliği.** Süit *bileşenleri* test ediyor, *teslimat zincirini* değil.
Testler dosyanın METNİNE bakıyor (`readFileSync`), Mami ÇİFT TIKLIYOR. Arada kalan her şey —
dosya izni, TTY varlığı, argüman aktarımı, süreç zinciri — tanımsız bölge.

Kanıtlar:
- `docsContract.test.ts:139-156` launcher'ı `read()` ile okuyup `toMatch(/node runner\.mjs/)`
  diyor — dosyayı asla ÇALIŞTIRMIYOR, iznine bakmıyor. 31 assertion'ın 13'ü string eşleme.
- Runner'ı spawn eden 8 test var; `--director` geçen **0**, `--launch` geçen **0**.
- `directorSession.test.ts:97` — `--director`'ü **runner'ı atlayıp** doğrudan runtime'a veriyor ve
  `--provider codex`'i **elle ekliyor**. Runner'ın bugün veremediği argümanı test kendi sağlıyor
  → H-04 duvarı görünmez kaldı.
- `beatMode: 'Manuel'` **hiçbir** test dosyasında yok (`qa.test.ts:145` sabit 'Dengeli') → H-02.
- `BASLAT` kelimesi hiçbir `.test.ts`'te geçmiyor; git'te mode `100644` → H-01.
- E2E `smoke.spec.ts:383` "Komut JSON"a basıyor ama kapı ZATEN AÇIK bir state ile; kapının
  yanlış kapandığı state hiç kurulmuyor.
- `.claude/test-baseline` yalnız SAYI sayıyor (2016→2046) — hangi katmanın kapandığını sormuyor,
  o yüzden sayı büyüdükçe güven büyüyor ama kapsam aynı yerde kalıyor.

**Sol'un en tehlikeli kör nokta uyarısı:** 69 sahnelik batch gerçek provider'la hiç ölçülmemiş.
`batchResilience.test.ts:282` / `commandRuntime.test.ts:614` sahte `claude`/`codex` binary'si
kullanıyor; fixture `sceneCount: 1` (`runnerGate.test.ts:19`). Birikimli context, kısmi çöküş ve
resume yalnız stub'la doğrulanmış. **Somut tahmin: batch'in sahne ~6-15 civarında sessizce durup
SAHNE-PROMPTLAR.md'yi yarım bırakması.** Bugün bunu canlı izliyoruz.

**Sol'un "neyi test etseydik bugün olmazdı" listesi (Mami seçecek):**
1. Çift tık simülasyonu: `statSync().mode & 0o111` + TTY'siz gerçek launcher koşusu → H-01+H-04
2. Runner'ın launch dalı: `--director`'ü runner ÜZERİNDEN, elle `--provider` eklemeden → H-04
3. Manuel elle-kesim senaryosu: `beatMode:'Manuel'` + boş satır silme → export açık mı → H-02
4. Kapı çelişki-kilidi: `sourceReadiness` (store) ile `evaluateDirectorCabinet` (qa) aynı state'te
   asla zıt hüküm veremez → H-02'nin kökü
5. E2E son-metre: Komut JSON indir → COMMAND-INBOX → TTY'siz launcher → SAHNE-PROMPTLAR.md oluştu mu

<!-- ŞABLON — her bulgu için kopyala
### H-01 · [SINIF] · kısa başlık

- **Adım:** (hangi ekran/komut/faz — brief / command / image_author / jury / motion / export)
- **Dünya / Motor:** 
- **Beklenen:** 
- **Olan:** 
- **Tekrar ediyor mu:** (1 kez / her seferinde / N/M turda)
- **Üretimi durdurdu mu:** evet / hayır — nasıl devam edildi
- **Log / ekran:**
```
(tam hata metni)
```
- **Not:** (varsa ilk şüphe — fix DEĞİL, sadece iz)
-->

---

## Gün sonu sınıflaması

_(gün sonunda doldurulacak — Mami seçer, onarım oturumu ayrı açılır)_
