# MAMILAS — clear sonrası kickoff (M1/M2 kök-B fix turu)

Dün üç fix turu bitti (24+ hata, hepsi push'lu: `41c179a`). İki makro bulgu KOŞARAK doğrulandı
ama kapatılamadı (usage bitti). Bu tur onları kapatıyoruz. **Önce oku:** memory
`project-mamilas-makro-ledger` + `mamilas-bul-sec-onar` (körleme yama + aptal regex YASAK) +
`EXECUTION_STATE.md` üst blok + `docs/ai/PROJECT_CONTRACT.md`.

Baseline: HEAD `41c179a` · tsc 0 · vitest 2016/2016 (80 dosya) · E2E 15/15 · mirror byte-identical.
gate.sh aktif (commit'te tsc/vitest/build koşar). Çalışma ağacı tertemiz.

İki bulgu da AYNI KÖK-B deseni: **"gate içeriği değil formatı doğruluyor"** — bir hash/verdict
FORMAT-geçerli diye kabul ediliyor, iddia ettiği içerikle ilişkisi doğrulanmıyor.

---

## TASK M1 — verifyProjectPack hash-içerik ankrajı (KRİTİK)

**Kök neden (dün koşarak kanıtlandı):** `src/core/projectPack.ts:233` beat.hash'i yalnız
`typeof beat.hash === 'string'` ile kontrol ediyor. beat.exactText='Su buharlaşır.' bırakıp
beat.hash=sourceHash('BAŞKA METİN') yapıp manifest'i yeniden mühürleyince → verify ok:true,
problems:[]. Sahte hash + hatta uydurma beat metni bile temiz geçiyor. Aynı kök
artifactHash/juryArtifactHash/protocolHash/storyboardHash'te de var (`:258-261` civarı — rastgele
format-geçerli hex geçiyor). TEK çalışan içerik-ankrajı: promptHash===sha256Hex(finalPrompt)
(`:257`) — bu ÇALIŞIYOR, örnek al.

**Yasa:** "Bir hash pack'te taşınıyorsa ve KAYNAĞI da pack'te varsa → verify onu HAM İÇERİKTEN
yeniden türetip eşitler (MOD-A). Kaynağı yoksa (frame pixel gibi) yalnız format kabul edilir ve
bu istisna AÇIKÇA işaretlenir (unverifiable damgası)."

**Yapılacak:**
1. `verifyProjectPack`'te her beat için `beat.hash === sourceHash(beat.exactText)` zorunlu kıl.
   `sourceHash` zaten `src/core/source.ts:86`'da export. Uyuşmazsa problems'e "Scene beat hash
   içerik ile uyuşmuyor" ekle.
2. Beat exactText bütünlüğü: reconstructed rawSource === pack.source.rawSource. `sourceIntegrity`
   (`source.ts`) zaten var ama verify onu HİÇ çağırmıyor — çağır ve ok:false ise problems'e yaz.
3. artifactHash/juryArtifactHash/protocolHash/storyboardHash: bunların KAYNAĞI pack'te taşınmıyor
   (yalnız hash var) → recompute imkânsız, MOD-B'de kalmak ZORUNDA. Bunları "doğrulanamayan kanıt"
   olarak açıkça etiketle (verify ok kalabilir ama pack'e/UI'ya "bu evidence zayıf/format-only"
   sinyali koy — sessiz güven YOK). Karar noktası: bu etiketi nasıl taşıyacağız, önce düşün.

**Kanıt zorunlu (KÖK, mamilas-test-suite-is-hollow dersi):** her yeni ankraj için FORGE-PROBE
mutasyon testi yaz — yalan hash → problems DOLU olmalı; format-only test hiçbir şeyi korumaz.
`src/core/projectPack.test.ts:245` zaten manifest kurcalıyor, oraya beat.hash içerik-yalanı +
artifact-hash forge testleri ekle. Dünkü 4 probe deseni: CONTROL(pass) / ATTACK-beatHashLie /
ATTACK-exactTextForge / FORGE-artifactHashes / PROMPTHASH_LIE(çalışan ankraj kontrastı).

---

## TASK M2 — jüri verdict öz-beyan çapraz-kontrolü (ORTA)

**Kök neden (dün koşarak kanıtlandı):** `src/core/agentProtocol.ts:288-300` `verifyAgentArtifact`
bir jüri (image/frame/motion) PASS'ini kabul etmek için yalnız (a) verdict enum'da, (b) evidence
boş-olmayan string dizisi (`:291`) bakıyor. evidence'ın yargıladığı prompt/frame ile İLGİSİ hiç
ölçülmüyor → `verdict:'PASS', evidence:['ok']` geçiyor. contentHash PROVENANCE'ı kanıtlar
(bu artifact bu context'ten türedi), CORRECTNESS'i (verdict hak edildi mi) değil.
requiredEvidence/rejectIf kontratı yazılıyor ama kod hiçbir maddeyi prompt'a karşı ÖLÇMÜYOR.

**Yasa:** "Jüri PASS dediyse, kodun ÖLÇEBİLDİĞİ rejectIf maddelerinden HİÇBİRİ prompt'ta
tetiklenmemeli — tetikleniyorsa artifact REDDEDİLİR. Ölçülemeyen maddeler agentAsserted:true
etiketiyle görünür kalır (sessiz güven yok)." Verdict "iddia" değil "kodla çapraz-kontrol edilmiş
iddia" olur.

**Yapılacak:**
1. `qa.ts` PROMPT SURGEON lint'i (hex sızıntısı, özne+ışık+kamera triad, motion klon tespiti —
   zaten nötr deterministik validator) — ama DİKKAT: SURGEON şu an ayrı export fonksiyon DEĞİL,
   `evaluateDirectorCabinet` içinde inline bir kol (qa.ts:309+). Önce onu **saf, çağrılabilir bir
   fonksiyona çıkar** (state değil, tek prompt string alıp findings dönen) — sonra hem cabinet hem
   verifyAgentArtifact onu çağırsın. Bu refactor davranışı DEĞİŞTİRMEMELİ (mevcut qa testleri
   yeşil kalmalı).
2. `verifyAgentArtifact` image_author/motion_author prompt'unu bu SURGEON'dan geçir: kod-ölçülebilir
   rejectIf tetiklenirse problems'e ekle. (Not: bu image_author için de geçerli — jüri PASS verse
   bile prompt hex/triad ihlali taşıyorsa kabul edilmemeli.)
3. Ölçülemeyen kontrat maddeleri için: verdict kabul edilir ama "agentAsserted" alanı/damgası ile
   işaretlenir. Nasıl taşınacağı karar noktası — önce düşün, gerekiyorsa Mami'ye sor.

**Kanıt zorunlu:** forge-probe — `verdict:'PASS' + evidence:['ok'] + hex sızan prompt` →
verifyAgentArtifact REDDETMELİ. Temiz prompt + PASS → kabul. Mevcut agentProtocol.test.ts'e ekle.

---

## YÜRÜTME SÖZLEŞMESİ (dünkü disiplin)

1. Her task: kök neden doğrula (probe koş) → kırmızı test yaz → fix → yeşil → **garanti denetçi
   ajanına** "bu fix kapattı mı + yeni kırık açtı mı" koşarak doğrulat.
2. Regex/keyword-tablo EKLEME — bu ikisi hash/verdict işi, regex değil. Site-hattı regex ölü
   kanalına DOKUNMA.
3. Test silme yok, mirror parity koru, sceneContextHash'i BOZMA (bozulursa tüm command'ler stale).
4. Bitince tam kapı (tsc/vitest/build/e2e/mirror) + tek commit + push. gate.sh commit'te koşacak.
5. Kalan iş (bu turda DEĞİL): makro ordu resume (`wf_3acfcb9a-12c` — KÖK-A/C tam analizi) +
   UI katmanı (58/100). Ledger'da.

Dürüst durum korunacak: implementation complete / visual validation pending — gerçek kare hükmü
Mami'nin.
