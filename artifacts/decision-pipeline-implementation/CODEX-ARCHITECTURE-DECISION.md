# MAMILAS — KEEP / CHANGE / REJECT mimari kararı

**Tarih:** 2026-07-15
**Otorite:** Mami'nin değişmez ürün sonucu + `CODEX-BASELINE-AUDIT.md` gerçek çıktısı.

## KEEP

- `commandId`/canonical decision hash, source bütünlüğü ve typed blocker omurgası.
- 46 WorldPacket'in `SURGERY_DATA.json`'dan türetilen fizik paketi; palette-as-light, compatible-ref
  ve legacy render law.
- Tek readiness, shot approval, gerçek frame SHA-256 + Mami verdict motion gate'i.
- Deterministik project pack/closeout fikri, ince Windows/macOS launcher'lar ve tek Node runtime.
- Site kullanıcı kararı/brief taşır; üretim API'si çağırmaz. Gerçek frame ve estetik verdict Mami'de.

## CHANGE

1. **Text kaynağı:** `AUTO` source-word heuristiği kaldırılır. Site source'tan yazı niyeti çıkarmaz.
   On-screen text yalnız açık Mami mode/directive/declaration'dan doğar; CLEAN kilidi aynen korunur.
2. **Site/author sınırı:** Legacy preview promptu uyumluluk için kalabilir, fakat aktif Image Author
   context'ine engine-prompt olarak girmez. Yeni minimum shot slice: canonical decision + approved
   shot + exact directives + WorldPacket + compatible refs + palette-as-light + locks + engine
   dialect + continuity. Final prompt yalnız Image Author artifact'idir.
3. **Tek protocol:** Karar yasası `agents/PROTOCOL.md` içinde sürümlü/content-hash'li yaşar.
   Claude/Codex adaptörleri yalnız I/O ve araç kullanımını anlatır; karar yasasını kopyalamaz.
4. **Artifact lifecycle:** Her artifact deterministic envelope taşır: protocol, role/provider,
   decision/storyboard/input hashes, content hash, phase ve revision. Faz verdict'i yalnız
   `PASS | REJECT | FACT_REQUIRED`; aynı phase'de en fazla bir targeted revision.
5. **On-demand roller:** Kullanıcı yalnız Yerleşik Yönetmen'i görür. Command her invocation'da
   yalnız sonraki geçerli uzmanı veya bağımsız jüriyi açar. Sabit swarm ve ajanlar arası loop yok.
6. **Deterministic sorumluluk:** Palette translation, IP firewall, schema/hash/stale, compatibility,
   engine math ve path safety kodda kalır. Palette/IP “ajanları” deprecated/non-runnable olur.
7. **Command:** Klasör/paket seçer → protocol/schema/active decision/gate/artifact doğrular → next
   role/context üretir → Mami seçerse tek interaktif Claude/Codex oturumu açar → çıkışta artifact'i
   doğrular. `--print`, giant JSON slice, otomatik provider/generation yok. `--dry-run` test kanıtıdır.
8. **Pack/evidence:** Project pack seçili palette'i taşır. Chromium E2E gerçek fixture upload/hash/
   APPROVE/motion-open ve stale akışını yapar; bu teknik kanıttır, visual PASS değildir.

## REJECT

- Kaynak kelimelerinden text/niyet çıkaran regex, NLP veya kelime-sayısı heuristiği.
- Altı-agent swarm, giant Production Agent, tekrar eden re-jüri ve kullanım/kredi ritüeli.
- Zorunlu Higgsfield/Magnific/provider/model söylemi; harici API, batch, otomatik generation/upscale.
- Frame yokken frame PASS, Mami `APPROVE` yokken motion, jüriyle Mami estetik hükmünü geçersiz kılma.
- İkinci runner, platforma/provider'a kopyalanmış karar yasası ve kör `claude --print` borusu.

**Karar:** Mevcut iyi deterministic omurga korunacak; “Macro 9 = daha çok ajan” hipotezi reddedildi.
Macro 9, tek Yerleşik Yönetmen yüzeyi altında **deterministic lifecycle + on-demand author/jury** olarak
uygulanacak. Mami onayı beklenmez.
