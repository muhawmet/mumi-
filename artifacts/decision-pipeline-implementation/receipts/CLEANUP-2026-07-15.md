# RECEIPT — Ürün-niyeti temizlikleri (izole, düşük risk)

- Tarih: 2026-07-15
- Model: Claude Opus 4.8
- Bağlam: Mami'nin ürün-niyeti bildirimi → `PRODUCT-INTENT-AUDIT.md`. Bu receipt, o denetimin
  **en düşük riskli, körleme gerektirmeyen** maddelerini kapatır. Büyük UI teardown'ları
  (Disco QA, sahte preview, tek readiness) **kasıtlı olarak sabaha bırakıldı** — Mami arayüzü
  görürken yapılmalı; kör söküm kırık arayüz teslim etme riski taşır.

## Ne silindi / düzeltildi

1. **`start-mamilas.command` — SİLİNDİ.** Sabit `/Users/Muhammet/Desktop/mamilas-modern` yolu
   (repo adı artık farklı) taşıyan ölü ikinci mac launcher. Taşınabilir karşılığı `BASLAT.command`
   zaten var. (`launcher-parity.md` drift #1.)
2. **`src/components/Advisors.ts` — SİLİNDİ.** Hiçbir yerde import edilmiyordu VE korumalı IP
   karakter adları döndürüyordu (`onepiece→sanji/zoro`, `demon_slayer→rengoku/tengen`). Ölü kod
   olarak durması bile telif firewall'un yasağıyla çelişiyordu.
3. **`scripts/start-codex.ps1:48` — DÜZELTİLDİ.** Tutmadığı `Model: gpt-5.6-sol | reasoning: high`
   banner'ını basıyordu ama `:36` hiçbir model argümanı geçirmiyor. Banner gerçeğe indirildi:
   model/reasoning Codex CLI oturumundan gelir. (drift #3.)
4. **`docs/ai/CODEX.md:12` — DÜZELTİLDİ.** *"Proje ayarları Sol + high reasoning kullanır"*
   yanlış iddiası kaldırıldı; gerçek: `config.toml` yalnız görsel/çoklu-ajan ayarlıyor.
   (`PROJECT_CONTRACT.md` zaten dürüsttü — dokunulmadı.)
5. **`src/components/Sidebar/Sidebar.tsx` (+ boş klasör) — SİLİNDİ.** 0 bayt, hiçbir yerden import
   edilmiyordu (`site-gates.md` doğruladı). tsc/build temiz.

## Doğrulama (GERÇEK)

| Komut | Sonuç |
|---|---|
| `npx tsc --noEmit` | **0 hata** |
| `docsContract.test.ts` | **97/97 geçti** (silmeler + doc düzeltmesi kilidi kırmadı) |
| `npm run build` | **OK** |

## Bilerek YAPILMADI (sabaha, Mami arayüzü görürken)

- **Disco QA / 26 ses + 7 persona → tek Yönetmen sesi.** UI'a derin bağlı (AppLayout,
  ProductionPulse, ThoughtDock, QAStep, useStudioStore, voicePortraits + testler). PROMPT SURGEON'ın
  hex/triad/motion-clone **validator çekirdeği KORUNACAK**, yalnız persona sunumu sökülecek.
- **Sahte preview (4 statik plaka + Canvas-2D prosedürel) → gerçek onaylı frame'e bağla ya da kaldır.**
- **Tek canonical readiness** — 9 rakip readiness/skor → typed blocker sonucu tek gerçek kapı.
  `PreviewStage.tsx:62` string-sniff PASS pill'i kaldırılacak.
- **Frame-hash motion gate (TASK 6 çekirdeği)** — `buildMotionPrompt` onaylı `frameHash` almadan
  yazamaz; `mamilas.receipt.v1` (TASK 2'de kuruldu) bağlanır.

Bunların hepsi `PRODUCT-INTENT-AUDIT.md`'de kök-neden + plan olarak duruyor.
