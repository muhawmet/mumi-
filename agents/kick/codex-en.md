# DEPRECATED — NON-RUNNABLE

This is a historical placeholder for the retired giant Production Agent path. It is not read by the runner and carries no retired provider rituals.

Use the canonical flow: export `*_mamilas_command.json`, run the thin cross-platform launcher, and let `scripts/mamilas-command.mjs` validate the command and open exactly one on-demand role. All decision law lives in the hashed `agents/PROTOCOL.md`.

Do not paste this file into an agent session.
